# Local development environment variables for the FlowForge backend.
# This file is gitignored and must never be committed.
#
# The key that used to live here was exposed (committed to the repo) and
# has been removed. Generate a NEW key at https://aistudio.google.com/apikey
# and paste it below — do not reuse the old one.
GEMINI_API_KEY=AQ.Ab8RN6KDo3DWIQr56GZzu702jNW1G5k4hFf8zckf7kj_RgtJKg

GEMINI_MODEL=gemini-3.6-flash
GEMINI_TIMEOUT_MS=100000
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:5173,http://127.0.0.1:5173
AI_PROVIDER=gemini
ENVIRONMENT=development
